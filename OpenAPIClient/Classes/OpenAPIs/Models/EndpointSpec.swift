//
// EndpointSpec.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Properties that can be configured to access and load balance a service. */
public struct EndpointSpec: Codable, JSONEncodable, Hashable {

    public enum Mode: String, Codable, CaseIterable {
        case vip = "vip"
        case dnsrr = "dnsrr"
    }
    /** The mode of resolution to use for internal load balancing between tasks.  */
    public var mode: Mode? = .vip
    /** List of exposed ports that this service is accessible on from the outside. Ports can only be provided if `vip` resolution mode is used.  */
    public var ports: [EndpointPortConfig]?

    public init(mode: Mode? = .vip, ports: [EndpointPortConfig]? = nil) {
        self.mode = mode
        self.ports = ports
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case mode = "Mode"
        case ports = "Ports"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(mode, forKey: .mode)
        try container.encodeIfPresent(ports, forKey: .ports)
    }
}

